import subprocess

# Activate the virtual environment (again, if needed)
activate_script = './env/bin/activate'
exec(open(activate_script).read(), dict(__file__=activate_script))

# Run tests (replace 'pytest' with your specific test command if needed)
subprocess.check_call(['pytest'])
